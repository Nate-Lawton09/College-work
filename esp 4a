import pandas as pd
import matplotlib.pyplot as plt

#Outputs the main menu and checks the user input
def main_menu():
    flag = True

    while flag:

        print("-"*66)
        print("---------- RetailX Sales Analysis Module ------------- ")
        print("-"*66)
        print("")
        print("--------------------- Main Menu --------------------- ")
        print("1. Total sales by product")
        print("2. Total profit by product")
        print("3. Total income by product")
        print("4. Sales comparision in a graph")
        print("5. Categories comparision in a pie chart")
 

        choice = input('Enter your number selection here: ')

        if choice.isdigit():
            flag = False
        else:
            flag = True

    return int(choice)

#Generates submenu of available product codes and allows user to select a product to view
def get_product_id ():

    df = pd.read_csv("Task4a_RetailX_data.csv")

    product_codes = df["Product ID"].unique().tolist()

    flag = True

    while flag:

        print("-"*66)
        print("---------- Product List ------------- ")
        print("-"*66)
        print("")
        #print("--------------------- Main Menu --------------------- ")
        print("Select a product code:")
        for i in range(len(product_codes)):
            print(i+1, " ", product_codes[i])

        selection = input('Enter your number selection here: ')

        if selection.isdigit():
            selection = int(selection)
            flag = False
        else:
            flag = True

        
        product_ID = product_codes[selection -1]
   
    print("You have selected product id:",product_ID)
    return product_ID

#gets and converts user input from string to date format
def get_date(start_end):
    
    flag = True
    
    while flag:
        date = input('Please enter {} date for your date range (DD/MM/YYYY) : '.format(start_end))

        try:
           pd.to_datetime(date, format="%d/%m/%Y")
        except:
            print("Sorry, you did not enter a valid date")
            flag = True
        else:
            flag = False
    
    return date

#extracts data based on product ID within a user specified date range.
def get_data_by_ID_and_date(product_id, start_date, end_date):
    all_data = pd.read_csv("Task4a_RetailX_data.csv")
    product_data = all_data.loc[all_data["Product ID"] == product_id].copy()

    product_data["Date"]= pd.to_datetime(product_data["Date"], format="%d/%m/%Y", errors="raise")
    
    date_range = (product_data["Date"] >= pd.to_datetime(start_date, format="%d/%m/%Y")) & \
                  (product_data["Date"] <= pd.to_datetime(end_date,format="%d/%m/%Y" ))
    
    extracted_data = product_data.loc[date_range]



    return extracted_data

#Collect and display categorie data
def category_comparision():
    all_data = pd.read_csv("Task4a_RetailX_data.csv")
    categories = []
    for i in range(len(all_data)):
        if all_data["Category"][i] not in categories:
            categories.append(all_data["Category"][i])
    print(categories)

    totals = [0]*len(categories)

    print("How would you like to compare categories?")
    print("1. Cost Price")
    print("2. Sales Price")
    print("3. Quantity Sold")

    while True:
        choice = input(">>")

        try:
            int(choice)
        except:
            print("Invalid input, try again")
        else:
            if choice == "1":
                comparision_choice = "Cost Price"
                break
            elif choice == "2":
                comparision_choice = "Sales Price"
                break
            elif choice == "3":
                comparision_choice = "Qty Sold"
                break
            else:
                print("Please select valid option")

    
    for i in range(len(categories)):
        for x in range(len(all_data)):
            if all_data["Category"][x] == categories[i]:
                totals[i] = totals[i] + all_data[comparision_choice][x]
        totals[i] = int(totals[i])
    
    print(totals)

    plt.pie(totals , labels=categories)
    plt.title(f"Pie chart comparing {comparision_choice} of each category")
    plt.show()

#generates a total of the number of items sold for the extracted data
def calculate_total_sale (date_ID, product_id, start_date, end_date):
    total_sales = date_ID["Qty Sold"].sum()
    print('The total number of sales for product {}, between {} and {} was: {}'.format(product_id, start_date, end_date, total_sales))

def calculate_total_profit (date_ID, product_id, start_date, end_date):
    total_sales = date_ID["Sales Price"].sum() - date_ID["Cost Price"].sum()
    print('The total number of profit for product {}, between {} and {} was: {}'.format(product_id, start_date, end_date, total_sales))

def calculate_total_income (date_ID, product_id, start_date, end_date):
    total_sales = date_ID["Sales Price"].sum()
    print('The total number of profit for product {}, between {} and {} was: {}'.format(product_id, start_date, end_date, total_sales))


main_menu_choice = main_menu()

if main_menu_choice == 1:
    product_id = get_product_id()
    start_date = get_date("start")
    end_date = get_date("end")
    date_ID = get_data_by_ID_and_date(product_id, start_date, end_date)
    calculate_total_sale (date_ID, product_id, start_date, end_date)

    print(date_ID)

elif main_menu_choice == 2:
    product_id = get_product_id()
    start_date = get_date("start")
    end_date = get_date("end")
    date_ID = get_data_by_ID_and_date(product_id, start_date, end_date)
    calculate_total_profit (date_ID, product_id, start_date, end_date)

elif main_menu_choice == 3:
    product_id = get_product_id()
    start_date = get_date("start")
    end_date = get_date("end")
    date_ID = get_data_by_ID_and_date(product_id, start_date, end_date)
    calculate_total_income (date_ID, product_id, start_date, end_date)

#Show chart comparing sales
elif main_menu_choice == 4:
    all_data = pd.read_csv("Task4a_RetailX_data.csv")
    
    product_id = []
    while True:
        product_id.append(get_product_id())

        print("Enter another product? (y/n)")
        confirm = input(">>")

        if confirm.lower() == "y":
            print("Select another item")
        else:
            
            print("Selection ended")
            break

    start_date = get_date("start")
    end_date = get_date("end")

    date_ID = []

    print("What would you like to view?")
    print("1. Cost Price")
    print("2. Sales Price")
    print("3. Quantity Sold")

    while True:
        choice = input(">>")

        try:
            int(choice)
        except:
            print("Invalid input, try again")
        else:
            if choice == "1":
                comparision_choice = "Cost Price"
                break
            elif choice == "2":
                comparision_choice = "Sales Price"
                break
            elif choice == "3":
                comparision_choice = "Qty Sold"
                break
            else:
                print("Please select valid option")

    for i in range(len(product_id)):
        date_ID = get_data_by_ID_and_date(product_id[i], start_date, end_date)
        print(date_ID)
        dates = date_ID["Date"]
        measure = date_ID[comparision_choice]
        
        
        
        plt.plot(dates , measure )
    plt.legend(product_id)
        
    
    plt.xlabel("Dates")
    plt.xticks(rotation = 30)
    plt.ylabel(comparision_choice)
    
    plt.title(f"Graph displaying {comparision_choice} of products")
    plt.show()


    

elif main_menu_choice == 5:
    category_comparision()
    


